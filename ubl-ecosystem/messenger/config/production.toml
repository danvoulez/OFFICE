# Messenger Production Configuration

[server]
host = "0.0.0.0"
port = 8081

[office]
endpoint = "http://office:8080"
timeout_ms = 30000

[ubl]
endpoint = "http://ubl:3000"
container_id = "messenger"
timeout_ms = 30000

[logging]
level = "info"
format = "json"
